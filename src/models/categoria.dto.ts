//Na pasta models é como se fosse a camada de dominio do frontend
export interface CategoriaDTO{ //No typescript para se definir uma categoria se utliza uma interface
    id : string;
    nome : string;
}